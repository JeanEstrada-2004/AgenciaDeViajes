@{
    ViewData["Title"] = "Confirmación de Cuenta";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.HideHeaderFooter = true;
}

@section Styles {
    <link rel="stylesheet" href="~/css/ConfirmaciónCuenta/style.css" asp-append-version="true"/>
}

<div class="confirmation-container">
    <div class="confirmation-card">
        <i class="confirmation-icon bi bi-check-circle-fill"></i> <!-- Usando Bootstrap Icons -->
        <h1 class="confirmation-title">Confirmación de Cuenta</h1>
        
        <div class="confirmation-message alert alert-info">
            @ViewBag.Mensaje
        </div>
        
        <a class="confirmation-button btn btn-primary" href="@Url.Action("Index", "Login")">
            <i class="bi bi-box-arrow-in-right me-2"></i>Ir a Iniciar Sesión
        </a>
    </div>
</div>